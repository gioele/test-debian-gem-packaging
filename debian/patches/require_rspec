Description: require RSpec in helper

--- ruby-filepath-0.6.orig/spec/spec_helper.rb
+++ ruby-filepath-0.6/spec/spec_helper.rb
@@ -3,6 +3,8 @@
 LIB_DIR = File.expand_path(File.join(File.dirname(__FILE__), %w[.. lib]))
 $LOAD_PATH.unshift(LIB_DIR) unless $LOAD_PATH.include?(LIB_DIR)
 
+require 'rspec'
+
 require 'filepath'
 
 RSpec.configure do |config|
